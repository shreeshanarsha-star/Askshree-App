export const metadata = { title: 'Leadership — Ask Shree writings' };
import LeadershipContentWrapper from './Content';

export default function LeadershipPage() {
  return <LeadershipContentWrapper />;
}
